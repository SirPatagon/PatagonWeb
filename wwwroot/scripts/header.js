components.push({
    name: 'sp-header', obj: {
        inject: ['globalProps'],
        props: ['arrowText'],
        template: getTemplate('header'),
        data() {
            return {
            };
        }
    }
});
